1. FindPrimes takes an integer, and returns all the prime numbers less than or equal to that integer. 
<br/>
2. It should return them separated by commas. The input 3, for example, should return "2, 3"